{
  "className":"sprouts.Composite_View_Building_Spec",
  "title":"Building Composite Views from Many Properties",
  "narrative":"The `Viewable.of(Val, Val, BiFunction)` factory methods let you merge exactly two\n    properties into a single live view. That is enough for a full name built from a\n    forename and a surname, but it does not scale: merging ten properties this way\n    means nesting nine composite views inside each other, each of which you have to\n    keep referenced, and whose combiner types quickly become unreadable.\n\n    This specification covers the scalable alternative: a composite view built from a\n    seed item and a chain of `join(..)` calls, where every `join` contributes one\n    property and one \"wither\" style combiner which folds the item of that property\n    into the composite item:\n\n    ```java\n    Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it\n            .join(cityProperty,        Weather::withCity)\n            .join(temperatureProperty, Weather::withTemperature)\n            .join(humidityProperty,    Weather::withHumidity)\n        );\n    ```\n\n    The item of such a composite view is always recomputed as a whole: the fold starts\n    at the seed and applies every combiner in `join` order, reading the *current* item\n    of every joined property. It never holds `null`, it degrades gracefully instead of\n    throwing when a combiner misbehaves, and just like every other view, it is only\n    weakly referenced by the properties it observes.",
  "subjects":["sprouts.Viewable","sprouts.Viewable$CompositeBuilder","sprouts.Val","sprouts.Var"],
  "statistics":{
    "runs":"39",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"Use the composite builder to merge many properties into a single view.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `Viewable.of(C, Function)` factory method takes a seed item and a\n            configurator lambda which receives a `Viewable.CompositeBuilder`.\n            Every `join(..)` call on that builder contributes one property together with\n            a combiner which folds the item of that property into the composite item.\n\n            The resulting `Viewable` is a live view: whenever any of the joined properties\n            changes, the composite item is recomputed and the change is propagated\n            to the listeners of the composite view.\n\n            Note how this scales to any number of properties without any nesting.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A handful of individual properties, as you would find them in a view model.","code":["Var<String>  city        = Var.of(\"Vienna\")","Var<Double>  temperature = Var.of(21.5d)","Var<Integer> humidity    = Var.of(55)","Var<Boolean> alert       = Var.of(false)","Var<String>  source      = Var.of(\"station-1\")"]},

        {"kind":"and","text":"We merge all five of them into a single composite view.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city,        Weather::withCity)","        .join(temperature, Weather::withTemperature)","        .join(humidity,    Weather::withHumidity)","        .join(alert,       Weather::withAlert)","        .join(source,      Weather::withSource)","    )"]},

        {"kind":"expect","text":"The composite view immediately holds the merged item.","code":["weather.get() == new Weather(\"Vienna\", 21.5d, 55, false, \"station-1\")"]},

        {"kind":"and","text":"It is a live view of the joined properties, and it never holds null.","code":["weather.isView()","!weather.allowsNull()"]},

        {"kind":"when","text":"We change one of the joined properties...","code":["temperature.set(28.5d)"]},

        {"kind":"then","text":"...only that part of the composite item is affected.","code":["weather.get() == new Weather(\"Vienna\", 28.5d, 55, false, \"station-1\")"]},

        {"kind":"when","text":"We change several of the joined properties...","code":["city.set(\"Graz\")","alert.set(true)"]},

        {"kind":"then","text":"...the composite item reflects all of them.","code":["weather.get() == new Weather(\"Graz\", 28.5d, 55, true, \"station-1\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view is folded from its seed, so parts which are not joined keep the seed item.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The item of a composite view is computed by starting at the seed and then\n            applying every combiner in `join` order. Any part of the seed which no\n            combiner ever touches therefore simply survives in the composite item.\n\n            This makes the seed the natural place to put constant or default state.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A single property and a seed which already carries some state.","code":["Var<String> city = Var.of(\"Vienna\")","var seed = new Weather(\"\", -1d, -1, true, \"manual-entry\")"]},

        {"kind":"and","text":"A composite view which only joins the city property.","code":["Viewable<Weather> weather = Viewable.of(seed, it -> it.join(city, Weather::withCity))"]},

        {"kind":"expect","text":"Only the city was replaced, everything else comes from the seed.","code":["weather.get() == new Weather(\"Vienna\", -1d, -1, true, \"manual-entry\")"]},

        {"kind":"when","text":"We change the joined property.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The un-joined parts of the seed are still intact.","code":["weather.get() == new Weather(\"Graz\", -1d, -1, true, \"manual-entry\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Build a composite view from a dynamic number of properties.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Because the join chain is expressed through a configurator lambda,\n            you are not restricted to a statically known set of properties.\n            You can loop over a collection of properties and join each of them,\n            which is the main reason this API exists.\n\n            This example also demonstrates the most important semantic guarantee of\n            a composite view: the item is **recomputed as a whole**, starting at the\n            seed, on every single change. An accumulating combiner like the one below\n            is only correct because of that. If the composite merely applied the\n            combiner of the property that changed to its current item, the sum would\n            drift further and further away from the truth with every change.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A dozen properties holding the numbers 1 to 12.","code":["var numbers = (1..12).collect({ Var.of(it) })"]},

        {"kind":"and","text":"A composite view which sums all of them by joining them in a loop.","code":["Viewable<Tally> tally = Viewable.of(new Tally(0), { builder ->","        numbers.inject(builder, { b, number ->","            b.join(number, (t, n) -> t.withTotal(t.total() + n))","        })","    })"]},

        {"kind":"expect","text":"The composite view holds the sum of the numbers 1 to 12.","code":["tally.get() == new Tally(78)"]},

        {"kind":"when","text":"We change the first of the twelve properties.","code":["numbers[0].set(100)"]},

        {"kind":"then","text":"The sum is recomputed from scratch, and not incrementally accumulated.","code":["tally.get() == new Tally(177)"]},

        {"kind":"when","text":"We change the last of the twelve properties as well.","code":["numbers[11].set(0)"]},

        {"kind":"then","text":"The sum is still exactly right.","code":["tally.get() == new Tally(165)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The order of the `join` calls defines the order in which the combiners are applied.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The combiners of a composite view form a fold which is applied in the exact\n            order in which the properties were joined. So if two combiners write to the\n            same part of the composite item, then the one which was joined last wins.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two properties which both want to determine the city of the weather item.","code":["Var<String> preferredCity = Var.of(\"Vienna\")","Var<String> fallbackCity  = Var.of(\"Graz\")"]},

        {"kind":"and","text":"A composite view where the fallback is joined last...","code":["Viewable<Weather> fallbackWins = Viewable.of(Weather.blank(), it -> it","        .join(preferredCity, Weather::withCity)","        .join(fallbackCity,  Weather::withCity)","    )"]},

        {"kind":"and","text":"...and another one where the preferred city is joined last.","code":["Viewable<Weather> preferredWins = Viewable.of(Weather.blank(), it -> it","        .join(fallbackCity,  Weather::withCity)","        .join(preferredCity, Weather::withCity)","    )"]},

        {"kind":"expect","text":"In both cases the combiner which was joined last determined the item.","code":["fallbackWins.get().city() == \"Graz\"","preferredWins.get().city() == \"Vienna\""]},

        {"kind":"when","text":"We change both properties.","code":["preferredCity.set(\"Linz\")","fallbackCity.set(\"Salzburg\")"]},

        {"kind":"then","text":"The join order still decides.","code":["fallbackWins.get().city() == \"Salzburg\"","preferredWins.get().city() == \"Linz\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The same property may be joined more than once.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A property is not required to be joined only once. Every `join` call is an\n            independent contribution to the fold, which means you can derive several\n            parts of the composite item from a single property.\n\n            The property itself is still observed only once though: the fold reads the\n            current item of every join anyway, so a second listener on the same property\n            would merely recompute the very same item a second time.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A single property...","code":["var city = Var.of(\"Vienna\")"]},

        {"kind":"and","text":"A composite view which derives three different parts from it.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city, Weather::withCity)","        .join(city, (w, c) -> w.withHumidity(c.length()))","        .join(city, (w, c) -> w.withAlert(c.startsWith(\"V\")))","    )"]},

        {"kind":"and","text":"A trace of the change events fired by the composite view.","code":["var trace = []","weather.onChange(From.ALL, { trace << it.currentValue().orElseNull() })"]},

        {"kind":"expect","text":"All three combiners contributed to the composite item.","code":["weather.get() == new Weather(\"Vienna\", 0d, 6, true, \"\")"]},

        {"kind":"and","text":"The property joined three times is still observed by a single listener.","code":["city.numberOfChangeListeners() == 1"]},

        {"kind":"when","text":"We change the property.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"All three parts of the composite item are updated...","code":["weather.get() == new Weather(\"Graz\", 0d, 4, false, \"\")"]},

        {"kind":"and","text":"...through exactly one change event, and not one per join.","code":["trace == [new Weather(\"Graz\", 0d, 4, false, \"\")]"]},

        {"kind":"when","text":"We force a change event on the property, without actually changing it.","code":["city.fireChange(From.ALL)"]},

        {"kind":"then","text":"The composite view also propagates that one exactly once.","code":["trace == [new Weather(\"Graz\", 0d, 4, false, \"\"), new Weather(\"Graz\", 0d, 4, false, \"\")]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view without any joins is an immutable property holding the seed.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A configurator which does not join anything is perfectly legal.\n            Since there is nothing which could ever change the composite item,\n            the resulting property is simply an immutable property holding the seed.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A composite view which joins nothing at all.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it)"]},

        {"kind":"expect","text":"It holds exactly the seed item.","code":["weather.get() == Weather.blank()"]},

        {"kind":"and","text":"And it is an immutable property, because nothing can ever change it.","code":["weather.isImmutable()","!weather.allowsNull()","weather.type() == Weather"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view of exclusively immutable properties is itself immutable.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Views of immutable properties are immutable themselves, because their item\n            can never change. A composite view applies the same optimization:\n            if every joined property is immutable, then so is the composite.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two immutable properties.","code":["Val<String>  city     = Val.of(\"Vienna\")","Val<Integer> humidity = Val.of(55)"]},

        {"kind":"and","text":"A composite view of the two of them.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city,     Weather::withCity)","        .join(humidity, Weather::withHumidity)","    )"]},

        {"kind":"expect","text":"The composite item was computed eagerly...","code":["weather.get() == new Weather(\"Vienna\", 0d, 55, false, \"\")"]},

        {"kind":"and","text":"...and the composite view is immutable, just like its sources.","code":["weather.isImmutable()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view of mutable and immutable properties only listens to the mutable ones.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Joining an immutable property is a useful way to fold a constant into the\n            composite item. Since such a property can never change, the composite view\n            does not need to observe it at all.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"One mutable and one immutable property.","code":["var city   = Var.of(\"Vienna\")","var source = Val.of(\"station-1\")"]},

        {"kind":"and","text":"A composite view of both of them.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city,   Weather::withCity)","        .join(source, Weather::withSource)","    )"]},

        {"kind":"expect","text":"The composite view is live, because one of its sources is mutable.","code":["weather.isView()","weather.get() == new Weather(\"Vienna\", 0d, 0, false, \"station-1\")"]},

        {"kind":"and","text":"Only the mutable property is observed.","code":["city.numberOfChangeListeners() == 1","source.numberOfChangeListeners() == 0"]},

        {"kind":"when","text":"We change the mutable property.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The composite item is updated, and the constant is still folded in.","code":["weather.get() == new Weather(\"Graz\", 0d, 0, false, \"station-1\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The item type of a composite view is the concrete type of its seed.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `Viewable.of(C, Function)` factory method has no way of knowing the\n            intended item type other than by looking at the seed. So the `type()` of the\n            resulting composite view is the concrete runtime class of the seed.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from a `Weather` seed.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"expect","text":"The type of the composite view is the type of the seed.","code":["weather.type() == Weather"]},

        {"kind":"and","text":"The composite view has no id, just like any other view.","code":["weather.id() == \"\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Use the `Class` based factory method to create a composite view of a polymorphic type.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When the item type of your composite view is polymorphic, then deriving the\n            type from the concrete class of the seed is not good enough: a combiner which\n            produces a sibling subtype would not fit into the composite view anymore.\n\n            The `Viewable.of(Class, C, Function)` factory method exists for exactly this\n            case: it pins the item type of the composite view to the supplied class,\n            so that any subtype of it may be stored.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property which decides which kind of shape we want, and one for its size.","code":["Var<String> kind = Var.of(\"rect\")","Var<Double> size = Var.of(2d)"]},

        {"kind":"and","text":"A composite view whose item type is explicitly declared to be the `Shape` supertype.","code":["Viewable<Shape> shape = Viewable.of(Shape.class, new Rect(1d, 1d), it -> it","        .join(kind, (s, k) -> \"circle\" == k ? new Circle(1d) : new Rect(1d, 1d))","        .join(size, (s, v) -> s instanceof Circle ? new Circle(v) : new Rect(v, v))","    )"]},

        {"kind":"expect","text":"The composite view has the declared type, and not the type of the seed.","code":["shape.type() == Shape","shape.get() == new Rect(2d, 2d)"]},

        {"kind":"when","text":"We switch to a completely different subtype of `Shape`.","code":["kind.set(\"circle\")"]},

        {"kind":"then","text":"The composite view happily accepts it.","code":["shape.get() == new Circle(2d)"]},

        {"kind":"when","text":"We change the size of the circle.","code":["size.set(7d)"]},

        {"kind":"then","text":"The composite item is updated as expected.","code":["shape.get() == new Circle(7d)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view retains its item when the fold produces an item of an incompatible type.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This is the flip side of the previous feature: if you use the factory method\n            without an explicit type, and your combiners produce a sibling subtype of the\n            seed type, then the resulting item does not fit into the composite view.\n\n            A composite view treats this exactly like any other failed recomputation:\n            it logs the problem and retains its previous item instead of throwing at\n            whoever happened to change a joined property.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We capture the `System.err` stream so that we can inspect the log.","code":["var originalErr = System.err","var outputStream = new ByteArrayOutputStream()","System.err = new PrintStream(outputStream)"]},

        {"kind":"and","text":"A property deciding the kind of shape, and a composite view without an explicit type.","code":["Var<String> kind = Var.of(\"rect\")","Viewable<Shape> shape = Viewable.of(new Rect(1d, 1d), it -> it","        .join(kind, (s, k) -> \"circle\" == k ? new Circle(1d) : new Rect(1d, 1d))","    )"]},

        {"kind":"expect","text":"The type of the composite view was inferred from the seed, so it is too narrow.","code":["shape.type() == Rect","shape.get() == new Rect(1d, 1d)"]},

        {"kind":"when","text":"We make the combiner produce a sibling subtype.","code":["kind.set(\"circle\")"]},

        {"kind":"then","text":"No exception reaches the caller...","code":["noExceptionThrown()"]},

        {"kind":"and","text":"...the composite view retained its previous item...","code":["shape.get() == new Rect(1d, 1d)"]},

        {"kind":"and","text":"...and the problem was logged.","code":["outputStream.toString().contains(\"Circle\")"]},

        {"kind":"cleanup","text":"We restore the original `System.err` stream.","code":["System.err = originalErr"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view never allows null items.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view is built from a non-null seed and it can only ever hold\n            non-null items. This is a deliberate design decision: a view is intended to\n            be consumed by an application layer, where `null` leads to exceptions and\n            ultimately to a confusing user experience.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"expect","text":"The composite view does not allow null items.","code":["!weather.allowsNull()","weather.isPresent()","!weather.isEmpty()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view is read-only.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view is a `Viewable`, which is a read-only property type, so there is no\n            way to set its item through the API you are handed. Its item is defined entirely by\n            the seed and the properties it was folded from, and the way to change it is to change\n            one of those properties.\n\n            Should you circumvent the type system to set it anyway, then this is rejected\n            explicitly instead of silently corrupting the composite item.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"when","text":"We try to set the item of the composite view directly.","code":["(weather as Var<Weather>).set(new Weather(\"Graz\", 1d, 2, true, \"manual\"))"]},

        {"kind":"then","text":"The attempt is rejected.","code":["thrown(UnsupportedOperationException)"]},

        {"kind":"and","text":"The composite view still holds the item it folded together.","code":["weather.get() == new Weather(\"Vienna\", 0d, 0, false, \"\")"]},

        {"kind":"when","text":"We change the property it was folded from instead.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The composite item is updated as expected.","code":["weather.get() == new Weather(\"Graz\", 0d, 0, false, \"\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Joined properties may be nullable, in which case the combiners receive `null`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view never holds `null` itself, but the properties it is composed\n            of may very well be nullable. Their items are handed to the combiners as they\n            are, which means a combiner has to be prepared to receive `null` when it is\n            joined to a nullable property.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A nullable property which is initially empty.","code":["Var<String> city = Var.ofNull(String)"]},

        {"kind":"and","text":"A composite view whose combiner maps `null` to a placeholder.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city, (w, c) -> w.withCity(c == null ? \"<unknown>\" : c))","    )"]},

        {"kind":"expect","text":"The combiner was called with `null` and produced the placeholder.","code":["weather.get().city() == \"<unknown>\""]},

        {"kind":"and","text":"The composite view is not nullable, even though its source is.","code":["city.allowsNull()","!weather.allowsNull()"]},

        {"kind":"when","text":"We give the nullable property an item.","code":["city.set(\"Vienna\")"]},

        {"kind":"then","text":"The composite item is updated.","code":["weather.get().city() == \"Vienna\""]},

        {"kind":"when","text":"We empty the nullable property again.","code":["city.set(null)"]},

        {"kind":"then","text":"The combiner receives `null` once more and maps it back to the placeholder.","code":["weather.get().city() == \"<unknown>\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Creating a composite view whose initial fold produces `null` throws a `NullPointerException`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Sprouts distinguishes between two phases: *deriving* a property, which is\n            when the reactive graph is being built, and *propagating* an event, which is\n            when the graph is already live.\n\n            Deriving must fail fast: if a combiner returns `null` while the composite\n            view is being created, then the composite view could not possibly honour its\n            promise of never holding `null`, and so its creation fails immediately.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property whose item makes the combiner below return `null`.","code":["Var<String> city = Var.of(\"\")"]},

        {"kind":"when","text":"We try to create a composite view with a combiner returning `null`.","code":["Viewable.of(Weather.blank(), it -> it","        .join(city, (w, c) -> c.isEmpty() ? null : w.withCity(c))","    )"]},

        {"kind":"then","text":"The creation of the composite view fails immediately.","code":["thrown(NullPointerException)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Creating a composite view whose initial fold throws, throws a `NullPointerException` and logs the cause.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Just like a combiner returning `null`, a combiner which throws while the\n            composite view is being created makes it impossible to compute an initial\n            item. The creation therefore fails fast as well, and the original exception\n            is logged so that you can find out what actually went wrong.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We capture the `System.err` stream so that we can inspect the log.","code":["var originalErr = System.err","var outputStream = new ByteArrayOutputStream()","System.err = new PrintStream(outputStream)"]},

        {"kind":"and","text":"A nullable property which is empty, and a combiner which cannot handle that.","code":["Var<String> city = Var.ofNull(String)"]},

        {"kind":"when","text":"We try to create a composite view with this null unaware combiner.","code":["Viewable.of(Weather.blank(), it -> it.join(city, (w, c) -> w.withCity(c.trim())))"]},

        {"kind":"then","text":"The creation of the composite view fails immediately.","code":["thrown(NullPointerException)"]},

        {"kind":"and","text":"The cause of the failure was logged.","code":["outputStream.toString().contains(\"NullPointerException\")"]},

        {"kind":"cleanup","text":"We restore the original `System.err` stream.","code":["System.err = originalErr"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A combiner returning `null` during a change is logged, and the composite view retains its item.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            While *deriving* a composite view fails fast, *propagating* a change must\n            degrade gracefully. A caller who merely changes a property does not expect\n            their control flow to be interrupted, and a view which is allowed to throw\n            at that point would leave the reactive graph in an inconsistent state.\n\n            So when a combiner returns `null` during a change, the composite view logs\n            the problem and keeps the last item it successfully computed.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We capture the `System.err` stream so that we can inspect the log.","code":["var originalErr = System.err","var outputStream = new ByteArrayOutputStream()","System.err = new PrintStream(outputStream)"]},

        {"kind":"and","text":"A property and a composite view whose combiner returns `null` for empty strings.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city, (w, c) -> c.isEmpty() ? null : w.withCity(c))","    )"]},

        {"kind":"expect","text":"Initially everything is fine.","code":["weather.get().city() == \"Vienna\"","outputStream.toString().isEmpty()"]},

        {"kind":"when","text":"We make the combiner return `null`.","code":["city.set(\"\")"]},

        {"kind":"then","text":"No exception reaches the caller...","code":["noExceptionThrown()"]},

        {"kind":"and","text":"...the composite view retained its previous item...","code":["weather.get().city() == \"Vienna\""]},

        {"kind":"and","text":"...and the problem was logged.","code":["!outputStream.toString().isEmpty()"]},

        {"kind":"when","text":"We give the property a usable item again.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The composite view recovers completely.","code":["weather.get().city() == \"Graz\""]},

        {"kind":"cleanup","text":"We restore the original `System.err` stream.","code":["System.err = originalErr"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A combiner throwing during a change is logged, and the composite view retains its item.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An exception thrown by one of the combiners of a composite view is caught and\n            logged instead of interrupting the control flow of whoever changed the joined\n            property. The composite view keeps the last item it successfully computed.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We capture the `System.err` stream so that we can inspect the log.","code":["var originalErr = System.err","var outputStream = new ByteArrayOutputStream()","System.err = new PrintStream(outputStream)"]},

        {"kind":"and","text":"A property and a composite view whose combiner validates its input.","code":["Var<Double> temperature = Var.of(21.5d)","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(temperature, (w, t) -> {","            if ( t < -273.15d )","                throw new IllegalArgumentException(\"Temperature below absolute zero!\")","            return w.withTemperature(t)","        })","    )"]},

        {"kind":"expect","text":"Initially everything is fine.","code":["weather.get().temperature() == 21.5d","outputStream.toString().isEmpty()"]},

        {"kind":"when","text":"We set an item which makes the combiner throw.","code":["temperature.set(-300d)"]},

        {"kind":"then","text":"We do not notice any exception being thrown!","code":["noExceptionThrown()"]},

        {"kind":"and","text":"The composite view still holds the last item it could compute.","code":["weather.get().temperature() == 21.5d"]},

        {"kind":"and","text":"Looking at the log, we see that the exception was logged.","code":["outputStream.toString().contains(\"Temperature below absolute zero!\")","outputStream.toString().contains(\"IllegalArgumentException\")"]},

        {"kind":"cleanup","text":"We restore the original `System.err` stream.","code":["System.err = originalErr"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The recomputation of a composite view is atomic.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The item of a composite view is folded together from the seed through all of\n            the combiners. If one of these combiners fails, then the whole recomputation\n            is discarded, and *not* the partial result of the combiners which ran before\n            the failing one. A composite view is therefore never observed in a\n            half updated state.\n\n            This also demonstrates the other half of the \"recompute from live items\"\n            semantic: once the failing combiner succeeds again, the composite view\n            catches up with *all* of the joined properties at once, including the\n            changes it had to discard in the meantime.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Three properties, where the middle one feeds a combiner which can fail.","code":["Var<String>  city        = Var.of(\"Vienna\")","Var<Double>  temperature = Var.of(21.5d)","Var<Integer> humidity    = Var.of(55)"]},

        {"kind":"and","text":"A composite view which folds them in exactly that order.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city, Weather::withCity)","        .join(temperature, (w, t) -> {","            if ( t < -273.15d )","                throw new IllegalArgumentException(\"Temperature below absolute zero!\")","            return w.withTemperature(t)","        })","        .join(humidity, Weather::withHumidity)","    )"]},

        {"kind":"expect","text":"Initially the composite view holds the merged item.","code":["weather.get() == new Weather(\"Vienna\", 21.5d, 55, false, \"\")"]},

        {"kind":"when","text":"We break the middle combiner.","code":["temperature.set(-300d)"]},

        {"kind":"then","text":"The composite view retains its item entirely.","code":["weather.get() == new Weather(\"Vienna\", 21.5d, 55, false, \"\")"]},

        {"kind":"when","text":"\n            We now change the property which is folded *before* the failing combiner.\n            If the fold were committed step by step, then the new city would leak into\n            the composite item even though the recomputation as a whole failed.\n        ","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"Nothing at all changed about the composite item.","code":["weather.get() == new Weather(\"Vienna\", 21.5d, 55, false, \"\")"]},

        {"kind":"when","text":"We also change the property which is folded *after* the failing combiner.","code":["humidity.set(80)"]},

        {"kind":"then","text":"The composite item is still completely untouched.","code":["weather.get() == new Weather(\"Vienna\", 21.5d, 55, false, \"\")"]},

        {"kind":"when","text":"We finally give the middle property a usable item again.","code":["temperature.set(19d)"]},

        {"kind":"then","text":"The composite view catches up with all of the changes it had to discard.","code":["weather.get() == new Weather(\"Graz\", 19d, 80, false, \"\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The composite view factory methods reject `null` arguments.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            None of the ingredients of a composite view may be `null`:\n            neither the seed, nor the type, nor the configurator, nor the property or\n            combiner of an individual join. And since the configurator is supposed to\n            return the builder it produced, returning `null` from it is rejected as well.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property we can use for joining.","code":["Var<String> city = Var.of(\"Vienna\")"]},

        {"kind":"when","text":"We pass a null seed...","code":["Viewable.of(null, it -> it)"]},

        {"kind":"then","text":"A null pointer exception is thrown.","code":["thrown(NullPointerException)"]},

        {"kind":"when","text":"We pass a null configurator...","code":["Viewable.of(Weather.blank(), null)"]},

        {"kind":"then","text":"A null pointer exception is thrown.","code":["thrown(NullPointerException)"]},

        {"kind":"when","text":"We pass a null type...","code":["Viewable.of(null, Weather.blank(), it -> it)"]},

        {"kind":"then","text":"A null pointer exception is thrown.","code":["thrown(NullPointerException)"]},

        {"kind":"when","text":"We return null from the configurator...","code":["Viewable.of(Weather.blank(), it -> null)"]},

        {"kind":"then","text":"A null pointer exception is thrown.","code":["thrown(NullPointerException)"]},

        {"kind":"when","text":"We join a null property...","code":["Viewable.of(Weather.blank(), it -> it.join(null, Weather::withCity))"]},

        {"kind":"then","text":"A null pointer exception is thrown.","code":["thrown(NullPointerException)"]},

        {"kind":"when","text":"We join a null combiner...","code":["Viewable.of(Weather.blank(), it -> it.join(city, null))"]},

        {"kind":"then","text":"A null pointer exception is thrown.","code":["thrown(NullPointerException)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view only fires a change event when its item actually changed.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view is not a mere event forwarder. It only notifies its own\n            listeners when the recomputed item actually differs from the previous one.\n            So a change in a joined property which does not survive the fold\n            stays invisible to the observers of the composite view.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property holding a temperature in degrees celsius.","code":["Var<Integer> celsius = Var.of(20)"]},

        {"kind":"and","text":"A composite view which merely derives an alert flag from it.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(celsius, (w, c) -> w.withAlert(c > 30))","    )"]},

        {"kind":"and","text":"A trace of all the change events of the composite view.","code":["var trace = []","weather.onChange(From.ALL, { trace << it.currentValue().orElseNull() })"]},

        {"kind":"when","text":"We change the property to another item which still maps to the same flag.","code":["celsius.set(25)"]},

        {"kind":"then","text":"The composite view did not fire a change event.","code":["trace.isEmpty()"]},

        {"kind":"and","text":"Its item is indeed unchanged.","code":["weather.get() == new Weather(\"\", 0d, 0, false, \"\")"]},

        {"kind":"when","text":"We change the property so that the derived flag flips.","code":["celsius.set(35)"]},

        {"kind":"then","text":"The composite view fired exactly one change event.","code":["trace == [new Weather(\"\", 0d, 0, true, \"\")]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A forced change event on a joined property is propagated by the composite view.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Sometimes you want to notify the observers of a property even though its item\n            did not change, which is what the `fireChange(Channel)` method is for.\n            A composite view honours such a forced change event and passes it on to its\n            own observers, even though its item did not change either.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"and","text":"A trace of all the change events of the composite view.","code":["var trace = []","weather.onChange(From.ALL, { trace << it.change() })"]},

        {"kind":"when","text":"We set the property to the very same item it already holds.","code":["city.set(\"Vienna\")"]},

        {"kind":"then","text":"Nothing happens at all.","code":["trace.isEmpty()"]},

        {"kind":"when","text":"We force a change event on the joined property.","code":["city.fireChange(From.ALL)"]},

        {"kind":"then","text":"The composite view forwarded the forced change event.","code":["trace == [SingleChange.NONE]"]},

        {"kind":"and","text":"Its item is of course still the same.","code":["weather.get().city() == \"Vienna\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view fires its change events on the channel of the originating change.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A change of a joined property carries the `Channel` it originated from,\n            and a composite view preserves that information when it notifies its own\n            observers. This way an observer of a composite view can still distinguish\n            a change which came from the view layer from one which came from the\n            view model layer.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"and","text":"Three traces, one for each of the three standard channels.","code":["var all = []","var fromView = []","var fromViewModel = []","weather.onChange(From.ALL,        { all << it.channel() })","weather.onChange(From.VIEW,       { fromView << it.channel() })","weather.onChange(From.VIEW_MODEL, { fromViewModel << it.channel() })"]},

        {"kind":"when","text":"We change the joined property through the view channel.","code":["city.set(From.VIEW, \"Graz\")"]},

        {"kind":"then","text":"Only the observers of the view channel and of all channels were notified.","code":["all == [From.VIEW]","fromView == [From.VIEW]","fromViewModel == []"]},

        {"kind":"when","text":"We change the joined property through the view model channel.","code":["city.set(From.VIEW_MODEL, \"Linz\")"]},

        {"kind":"then","text":"Only the observers of the view model channel and of all channels were notified.","code":["all == [From.VIEW, From.VIEW_MODEL]","fromView == [From.VIEW]","fromViewModel == [From.VIEW_MODEL]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view of properties which share a common source is free of intermediate items.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            It is very common to join properties which are not actually independent of\n            each other, for example a property together with a view derived from it.\n            A naive implementation would then expose intermediate items to its observers:\n            one where the first property is already updated but the derived one is not.\n\n            A composite view avoids this entirely, because it never remembers the items\n            of its sources. Every recomputation reads the *current* item of every joined\n            property, and by the time a composite view is notified, all of its sources\n            have already been brought up to date.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property, and a chain of two views derived from it.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Integer> nameLength = city.viewAsInt( c -> c.length() )","Viewable<Boolean> hasLongName = nameLength.viewAs( Boolean.class, n -> n > 5 )"]},

        {"kind":"and","text":"A composite view joining the property together with both of its derived views.","code":["Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city,        Weather::withCity)","        .join(nameLength,  Weather::withHumidity)","        .join(hasLongName, Weather::withAlert)","    )"]},

        {"kind":"and","text":"A trace of all the change events of the composite view.","code":["var trace = []","weather.onChange(From.ALL, { trace << it.currentValue().orElseNull() })"]},

        {"kind":"expect","text":"The composite view starts out consistent.","code":["weather.get() == new Weather(\"Vienna\", 0d, 6, true, \"\")"]},

        {"kind":"when","text":"We change the single property all three sources ultimately depend on.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"Exactly one change event was fired, and it carries a fully consistent item.","code":["trace == [new Weather(\"Graz\", 0d, 4, false, \"\")]"]},

        {"kind":"and","text":"The composite view holds that same consistent item.","code":["weather.get() == new Weather(\"Graz\", 0d, 4, false, \"\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Composite views can be joined into other composite views.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view is an ordinary `Viewable`, which means it can be used\n            wherever a `Val` is expected, including as a source of another composite view.\n            This lets you assemble large models out of smaller, independently\n            defined composites.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two properties which are merged into a first composite view.","code":["Var<String>  city     = Var.of(\"Vienna\")","Var<Integer> humidity = Var.of(55)","Viewable<Weather> location = Viewable.of(Weather.blank(), it -> it","        .join(city,     Weather::withCity)","        .join(humidity, Weather::withHumidity)","    )"]},

        {"kind":"and","text":"A second composite view which joins the first one together with another property.","code":["Var<Double> temperature = Var.of(21.5d)","Viewable<Weather> complete = Viewable.of(Weather.blank(), it -> it","        .join(location,    (w, l) -> w.withCity(l.city()).withHumidity(l.humidity()))","        .join(temperature, Weather::withTemperature)","    )"]},

        {"kind":"expect","text":"The outer composite view merged everything.","code":["complete.get() == new Weather(\"Vienna\", 21.5d, 55, false, \"\")"]},

        {"kind":"when","text":"We change a property of the inner composite view.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The change propagates all the way through both composite views.","code":["location.get().city() == \"Graz\"","complete.get() == new Weather(\"Graz\", 21.5d, 55, false, \"\")"]},

        {"kind":"when","text":"We change the property of the outer composite view.","code":["temperature.set(28d)"]},

        {"kind":"then","text":"Only the outer composite view is affected.","code":["location.get() == new Weather(\"Graz\", 0d, 55, false, \"\")","complete.get() == new Weather(\"Graz\", 28d, 55, false, \"\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Every `join` registers exactly one change listener on the joined property.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view observes each joined property with a single change listener.\n            You can verify this through the number of change listeners reported by the\n            joined properties.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Three properties without any listeners.","code":["var city        = Var.of(\"Vienna\")","var temperature = Var.of(21.5d)","var humidity    = Var.of(55)"]},

        {"kind":"expect","text":"Initially there are no change listeners registered.","code":["city.numberOfChangeListeners() == 0","temperature.numberOfChangeListeners() == 0","humidity.numberOfChangeListeners() == 0"]},

        {"kind":"when","text":"We create a composite view joining the first two properties.","code":["var weather = Viewable.of(Weather.blank(), it -> it","        .join(city,        Weather::withCity)","        .join(temperature, Weather::withTemperature)","    )"]},

        {"kind":"then","text":"Exactly the two joined properties are being observed.","code":["city.numberOfChangeListeners() == 1","temperature.numberOfChangeListeners() == 1","humidity.numberOfChangeListeners() == 0"]},

        {"kind":"when","text":"We create a second composite view joining all three properties.","code":["var otherWeather = Viewable.of(Weather.blank(), it -> it","        .join(city,        Weather::withCity)","        .join(temperature, Weather::withTemperature)","        .join(humidity,    Weather::withHumidity)","    )"]},

        {"kind":"then","text":"Each composite view contributes its own listeners.","code":["city.numberOfChangeListeners() == 2","temperature.numberOfChangeListeners() == 2","humidity.numberOfChangeListeners() == 1"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view and its listeners are garbage collected when it is no longer referenced strongly.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Just like every other view in Sprouts, a composite view is only weakly\n            referenced by the properties it observes. So when you stop referencing a\n            composite view, it becomes eligible for garbage collection together with all\n            of the change listeners registered on it, and the change listeners it\n            registered on its joined properties disappear as well.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two properties without any listeners.","code":["var city     = Var.of(\"Vienna\")","var humidity = Var.of(55)"]},

        {"kind":"expect","text":"Initially there are no change listeners registered.","code":["city.numberOfChangeListeners() == 0","humidity.numberOfChangeListeners() == 0"]},

        {"kind":"when","text":"We create a composite view which we reference strongly.","code":["var strongly = Viewable.of(Weather.blank(), it -> it","        .join(city,     Weather::withCity)","        .join(humidity, Weather::withHumidity)","    )"]},

        {"kind":"then","text":"Both joined properties are being observed.","code":["city.numberOfChangeListeners() == 1","humidity.numberOfChangeListeners() == 1"]},

        {"kind":"when","text":"We create two more composite views which we do not reference strongly.","code":["Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity).join(humidity, Weather::withHumidity))","Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity).join(humidity, Weather::withHumidity))"]},

        {"kind":"and","text":"We wait for the garbage collector to run.","code":["waitForGarbageCollection()","Thread.sleep(500)","waitForGarbageCollection()"]},

        {"kind":"then","text":"Only the listeners of the strongly referenced composite view survived.","code":["city.numberOfChangeListeners() == 1","humidity.numberOfChangeListeners() == 1"]},

        {"kind":"and","text":"The surviving composite view still works.","code":["strongly.get() == new Weather(\"Vienna\", 0d, 55, false, \"\")"]},

        {"kind":"when","text":"We change one of the joined properties.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The surviving composite view is updated.","code":["strongly.get().city() == \"Graz\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view keeps the views it joined alive.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A property only holds a weak reference to the views derived from it, which is\n            what makes views memory leak safe. But that also means somebody has to keep\n            an intermediate view alive for as long as it is needed.\n\n            A composite view does exactly that: it references a joined view or lens\n            *strongly*, so that you may create intermediate properties inline inside the\n            configurator without having to store them yourself.\n\n            Note that this does **not** apply to plain properties, which a composite view\n            references weakly, exactly like every other view does. Have a look at the\n            \"Composite View Memory Safety\" specification for the full picture.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A regular property which we reference strongly.","code":["var city = Var.of(\"Vienna\")"]},

        {"kind":"and","text":"A composite view joining a view which is created inline and referenced nowhere else.","code":["var weather = Viewable.of(Weather.blank(), it -> it","        .join(city.viewAsInt( c -> c.length() ), Weather::withHumidity)","    )"]},

        {"kind":"expect","text":"The inline view was folded into the composite item.","code":["weather.get().humidity() == 6"]},

        {"kind":"when","text":"We wait for the garbage collector to run.","code":["waitForGarbageCollection()","Thread.sleep(500)","waitForGarbageCollection()"]},

        {"kind":"then","text":"The inline view was not collected, because the composite view keeps it alive.","code":["city.numberOfChangeListeners() == 1"]},

        {"kind":"when","text":"We change the property the inline view is derived from.","code":["city.set(\"Sankt Poelten\")"]},

        {"kind":"then","text":"The composite view is updated through the still living inline view.","code":["weather.get().humidity() == 13"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The composite builder is immutable, so joins on an escaped builder have no effect.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `Viewable.CompositeBuilder` is an immutable value: every `join` call\n            returns a *new* builder instead of mutating the one it was called on, and no\n            change listener is registered before the configurator has returned.\n\n            This means a builder which escapes from the configurator is completely inert.\n            Calling `join` on it cannot retroactively modify an already created composite\n            view, and it cannot secretly register listeners on your properties either.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two properties, of which we only intend to join the first one.","code":["var city        = Var.of(\"Vienna\")","var temperature = Var.of(21.5d)"]},

        {"kind":"and","text":"A composite view whose configurator lets the builder escape.","code":["var escaped = null","var weather = Viewable.of(Weather.blank(), { builder ->","        escaped = builder","        return builder.join(city, Weather::withCity)","    })"]},

        {"kind":"expect","text":"The composite view was built as expected.","code":["weather.get() == new Weather(\"Vienna\", 0d, 0, false, \"\")","city.numberOfChangeListeners() == 1","temperature.numberOfChangeListeners() == 0"]},

        {"kind":"when","text":"We use the escaped builder to join the second property after the fact.","code":["escaped.join(temperature, Weather::withTemperature)"]},

        {"kind":"then","text":"No listener was registered on the second property.","code":["temperature.numberOfChangeListeners() == 0"]},

        {"kind":"and","text":"The already created composite view is unaffected.","code":["weather.get() == new Weather(\"Vienna\", 0d, 0, false, \"\")"]},

        {"kind":"when","text":"We change the property which was never really joined.","code":["temperature.set(30d)"]},

        {"kind":"then","text":"The composite view does not notice at all.","code":["weather.get() == new Weather(\"Vienna\", 0d, 0, false, \"\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Give a composite view an id using the `withId(..)` method.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view has no id by default, but just like any other property you can\n            derive a named copy of it through the `withId(..)` method. The named copy folds\n            the very same properties, which means it is a live view in its own right.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["var city = Var.of(\"Vienna\")","var weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"and","text":"A named copy of the composite view.","code":["var named = weather.withId(\"weather\")"]},

        {"kind":"expect","text":"The copy carries the id, while the original still has none.","code":["named.id() == \"weather\"","weather.id() == \"\""]},

        {"kind":"and","text":"Both of them hold the same item.","code":["named.get() == weather.get()"]},

        {"kind":"when","text":"We change the joined property.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"Both the original and the named copy are updated.","code":["weather.get().city() == \"Graz\"","named.get().city() == \"Graz\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view has a descriptive string representation.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `toString()` method of a composite view tells you that you are looking at a\n            view, together with the item type and the current item. If the view has an id,\n            then that is part of the string representation as well.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["var city = Var.of(\"Vienna\")","var weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"expect","text":"The composite view presents itself as a view of the seed type.","code":["weather.toString().startsWith(\"View<Weather>[\")","weather.toString().contains(\"city=Vienna\")"]},

        {"kind":"and","text":"A named composite view mentions its id as well.","code":["weather.withId(\"weather\").toString().startsWith(\"View<Weather>[weather=\")"]},

        {"kind":"when","text":"We change the joined property.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"The string representation reflects the new item.","code":["weather.toString().contains(\"city=Graz\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can fire a change event on a composite view manually.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Sometimes you want to notify the observers of a composite view even though nothing\n            changed, which is what the `fireChange(Channel)` method inherited from `Val` is for.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["Var<String> city = Var.of(\"Vienna\")","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"and","text":"A trace of all the change events of the composite view.","code":["var trace = []","weather.onChange(From.ALL, { trace << it.change() })"]},

        {"kind":"when","text":"We fire a change event on the composite view itself.","code":["weather.fireChange(From.ALL)"]},

        {"kind":"then","text":"The observers were notified, even though nothing changed.","code":["trace == [SingleChange.NONE]"]},

        {"kind":"and","text":"The item of the composite view is of course unchanged.","code":["weather.get() == new Weather(\"Vienna\", 0d, 0, false, \"\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can unsubscribe the observers of a composite view.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The observers of a composite view can be removed individually through\n            `unsubscribe(Subscriber)`, or all at once through `unsubscribeAll()`,\n            exactly like on any other `Viewable`.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["var city = Var.of(\"Vienna\")","var weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"and","text":"Two observers writing into their own trace.","code":["var traceA = []","var traceB = []","Action<ValDelegate<Weather>> observerA = { traceA << it.currentValue().orElseNull().city() }","Action<ValDelegate<Weather>> observerB = { traceB << it.currentValue().orElseNull().city() }","weather.onChange(From.ALL, observerA)","weather.onChange(From.ALL, observerB)"]},

        {"kind":"expect","text":"The composite view has two observers.","code":["weather.numberOfChangeListeners() == 2"]},

        {"kind":"when","text":"We change the joined property.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"Both observers were notified.","code":["traceA == [\"Graz\"]","traceB == [\"Graz\"]"]},

        {"kind":"when","text":"We unsubscribe the first observer and change the property again.","code":["weather.unsubscribe(observerA)","city.set(\"Linz\")"]},

        {"kind":"then","text":"Only the second observer was notified.","code":["weather.numberOfChangeListeners() == 1","traceA == [\"Graz\"]","traceB == [\"Graz\", \"Linz\"]"]},

        {"kind":"when","text":"We unsubscribe everything and change the property one last time.","code":["weather.unsubscribeAll()","city.set(\"Salzburg\")"]},

        {"kind":"then","text":"Nothing is notified anymore.","code":["weather.numberOfChangeListeners() == 0","traceA == [\"Graz\"]","traceB == [\"Graz\", \"Linz\"]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view can be observed through a simple `Observer`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Besides the `onChange(Channel, Action)` method, a composite view is also a plain\n            `Observable`, which means you can subscribe an `Observer` to it if you are only\n            interested in the fact that something changed, and not in what exactly.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A property and a composite view built from it.","code":["var city = Var.of(\"Vienna\")","var weather = Viewable.of(Weather.blank(), it -> it.join(city, Weather::withCity))"]},

        {"kind":"and","text":"An observer counting how often the composite view changed.","code":["var count = 0","weather.subscribe({ count++ } as Observer)"]},

        {"kind":"when","text":"We change the joined property twice.","code":["city.set(\"Graz\")","city.set(\"Linz\")"]},

        {"kind":"then","text":"The observer was notified twice.","code":["count == 2"]},

        {"kind":"when","text":"We set the property to the item it already holds.","code":["city.set(\"Linz\")"]},

        {"kind":"then","text":"Nothing happened, because the composite item did not change.","code":["count == 2"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can create ordinary property views from a composite view.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A composite view is an ordinary `Viewable`, so all the usual view methods work on\n            it as well. This is how you narrow a large composite item back down to the single\n            value some part of your user interface actually needs.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two properties merged into a composite view.","code":["Var<String>  city     = Var.of(\"Vienna\")","Var<Integer> humidity = Var.of(55)","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city,     Weather::withCity)","        .join(humidity, Weather::withHumidity)","    )"]},

        {"kind":"and","text":"A couple of ordinary views derived from the composite view.","code":["Viewable<String>  summary = weather.viewAsString( w -> w.city() + \" @ \" + w.humidity() + \"%\" )","Viewable<Boolean> humid   = weather.viewAs( Boolean.class, w -> w.humidity() > 50 )"]},

        {"kind":"expect","text":"The derived views hold what the composite item says.","code":["summary.get() == \"Vienna @ 55%\"","humid.get() == true"]},

        {"kind":"when","text":"We change one of the properties the composite view was folded from.","code":["humidity.set(30)"]},

        {"kind":"then","text":"The change propagates through the composite view into the derived views.","code":["summary.get() == \"Vienna @ 30%\"","humid.get() == false"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A composite view tolerates changes made from within its own observers.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An observer of a composite view may very well change one of the properties that\n            same composite view was folded from. Such a re-entrant change simply triggers\n            another recomputation, and as long as your observer eventually stops changing\n            things, the composite view settles on a consistent item.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two properties merged into a composite view.","code":["Var<String>  city     = Var.of(\"Vienna\")","Var<Integer> humidity = Var.of(0)","Viewable<Weather> weather = Viewable.of(Weather.blank(), it -> it","        .join(city,     Weather::withCity)","        .join(humidity, Weather::withHumidity)","    )"]},

        {"kind":"and","text":"An observer which keeps raising the humidity until it reaches 3.","code":["weather.onChange(From.ALL, {","    if ( it.currentValue().orElseNull().humidity() < 3 )","        humidity.set(humidity.get() + 1)","})"]},

        {"kind":"when","text":"We change the other property, which sets the whole cascade in motion.","code":["city.set(\"Graz\")"]},

        {"kind":"then","text":"No exception escaped, and the recursion terminated.","code":["noExceptionThrown()"]},

        {"kind":"and","text":"The composite view settled on a consistent item.","code":["weather.get() == new Weather(\"Graz\", 0d, 3, false, \"\")"]},

        {"kind":"and","text":"The properties it was folded from agree with it.","code":["city.get() == \"Graz\"","humidity.get() == 3"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"An immutable composite view built with an explicit type keeps that type.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The collapse of a composite view into an immutable property does not lose the\n            type you declared: an immutable composite view built through the `Class` based\n            factory method reports the declared type, and not the concrete type of the seed.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A composite view of a polymorphic type which joins nothing at all.","code":["Viewable<Shape> shape = Viewable.of(Shape.class, new Rect(1d, 1d), it -> it)"]},

        {"kind":"expect","text":"It collapsed into an immutable property...","code":["shape.isImmutable()"]},

        {"kind":"and","text":"...which nevertheless reports the declared supertype.","code":["shape.type() == Shape","shape.get() == new Rect(1d, 1d)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A seed which does not fit the declared type is rejected.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `Class` based factory method declares the item type of the composite view,\n            so a seed which is not an instance of that type could never be folded into a\n            valid item and is rejected right away.\n        "]
      },
      "blocks":[
        {"kind":"when","text":"We try to build a composite view whose seed does not fit the declared type.","code":["Viewable.of(Shape.class, \"I am not a shape\", it -> it)"]},

        {"kind":"then","text":"The attempt is rejected.","code":["var exception = thrown(IllegalArgumentException)"]},

        {"kind":"and","text":"The error message explains the mismatch.","code":["exception.message.contains(\"Shape\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The configurator has to return the builder it was given.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `Viewable.CompositeBuilder` handed to your configurator is the only builder a\n            composite view can be built from, because it is the one collecting your joins.\n            Returning some other implementation of the interface is therefore rejected.\n        "]
      },
      "blocks":[
        {"kind":"when","text":"We return a foreign builder implementation from the configurator.","code":["Viewable.of(Weather.blank(), it -> ({ property, combiner -> null } as Viewable.CompositeBuilder))"]},

        {"kind":"then","text":"The attempt is rejected.","code":["var exception = thrown(IllegalArgumentException)"]},

        {"kind":"and","text":"The error message tells us what to do instead.","code":["exception.message.contains(\"join\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
